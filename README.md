# SG_proj_014

Central Orchestrator module for chaining APIs (011, 012, 013).

## Directory Structure
- `src/`: Main application code
- `configs/`: Configuration files
- `tests/`: Unit and integration tests

## Run
```bash
uvicorn src.main:app --host 0.0.0.0 --port 8014 --reload
```
